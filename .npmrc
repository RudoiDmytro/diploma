# React 19 transition: several still-declared `react@^18` peer ranges on
# transitive deps would otherwise ERESOLVE. They all work on React 19 at
# runtime. This keeps `npm install` and Docker `npm ci` working without flags.
legacy-peer-deps=true
